import React, { useEffect, useState } from 'react';
import { supabase } from '../supabase';
import { saveSiteSetting } from '../utils/siteSettingsHelper';
import { Image as ImageIcon, Loader2, Save, Upload } from 'lucide-react';
import Swal from 'sweetalert2';

interface SambutanConfig { nama:string; jabatan:string; foto_url:string; isi_sambutan?:string; paragraf_1?:string; paragraf_2?:string; paragraf_3?:string; }
const FALLBACK='https://missjyvqfehamtpyodjr.supabase.co/storage/v1/object/public/identitas-atlet/identitas/1775222807673-ccq2ee.jpg';
const DEFAULT:SambutanConfig={nama:'H. Wawan',jabatan:'Ketua Umum PB Bilibili 162',foto_url:FALLBACK,isi_sambutan:'Selamat datang di PB Bilibili 162. Kami menyambut hangat seluruh atlet bulutangkis dan para pecinta olahraga bulutangkis di Kota Parepare. Kehadiran Anda adalah semangat bagi kami untuk terus berkontribusi bagi kemajuan bulutangkis di daerah kita tercinta.\n\nBagi rekan-rekan atlet, kami berkomitmen menyediakan wadah pelatihan yang terstruktur, disiplin, dan berintegritas untuk mengasah potensi maksimal Anda. Sementara bagi seluruh pecinta bulutangkis di Parepare, mari kita jadikan klub ini sebagai rumah bersama dalam memupuk sportivitas dan kegemaran terhadap olahraga ini.\n\nMari kita terus bersinergi, meraih prestasi gemilang, dan mempererat tali persaudaraan di dalam maupun di luar lapangan. Terima kasih atas dukungan dan kepercayaan yang Anda berikan kepada PB Bilibili 162.'};

const joinLegacy=(v:any)=>{ if(v?.isi_sambutan) return String(v.isi_sambutan); return [v?.paragraf_1,v?.paragraf_2,v?.paragraf_3].filter((x)=>String(x||'').trim()).join('\n\n'); };
const splitParagraphs=(text:string)=>text.split(/\n\s*\n+/).map(x=>x.replace(/\s*\n\s*/g,' ').trim()).filter(Boolean);

export default function AdminSambutanKetua(){
 const [form,setForm]=useState<SambutanConfig>(DEFAULT),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[uploading,setUploading]=useState(false);
 const loadData=async()=>{try{const {data,error}=await supabase.from('site_settings').select('value').eq('key','sambutan_ketua_umum').maybeSingle();if(error)throw error;if(data?.value){const v=typeof data.value==='string'?JSON.parse(data.value):data.value;setForm({...DEFAULT,...v,isi_sambutan:joinLegacy(v)});}}catch(e){console.error(e);Swal.fire({icon:'error',title:'Gagal Memuat',text:'Data sambutan tidak dapat dimuat dari Supabase.',background:'#0F172A',color:'#fff'});}finally{setLoading(false);}};
 useEffect(()=>{loadData();const c=supabase.channel('admin-sambutan-ketua-realtime').on('postgres_changes',{event:'*',schema:'public',table:'site_settings',filter:'key=eq.sambutan_ketua_umum'},()=>loadData()).subscribe();return()=>{supabase.removeChannel(c)};},[]);
 const uploadPhoto=async(file:File)=>{setUploading(true);try{const ext=file.name.split('.').pop()?.toLowerCase()||'jpg',path=`sambutan-ketua/ketua-${Date.now()}.${ext}`;const {error}=await supabase.storage.from('uploads').upload(path,file,{upsert:false});if(error)throw error;const {data}=supabase.storage.from('uploads').getPublicUrl(path);setForm(p=>({...p,foto_url:data.publicUrl}));Swal.fire({toast:true,position:'top-end',icon:'success',title:'Foto berhasil diunggah',showConfirmButton:false,timer:1800,background:'#1E293B',color:'#fff'});}catch(e:any){Swal.fire({icon:'error',title:'Upload Gagal',text:e?.message||'Foto gagal diunggah.',background:'#0F172A',color:'#fff'});}finally{setUploading(false)}};
 const update=(field:keyof SambutanConfig,value:string)=>setForm(p=>({...p,[field]:value}));
 const handleSave=async(e:React.FormEvent)=>{e.preventDefault();if(!form.nama?.trim())return Swal.fire('Perhatian','Nama Ketua Umum wajib diisi.','warning');if(!form.foto_url?.trim())return Swal.fire('Perhatian','Foto Ketua Umum wajib tersedia.','warning');if(!form.isi_sambutan?.trim())return Swal.fire('Perhatian','Isi sambutan wajib diisi.','warning');setSaving(true);try{const paragraphs=splitParagraphs(form.isi_sambutan);const payload={...form,nama:form.nama.trim(),jabatan:(form.jabatan||'').trim(),foto_url:form.foto_url.trim(),isi_sambutan:form.isi_sambutan.trim(),paragraf_1:paragraphs[0]||'',paragraf_2:paragraphs[1]||'',paragraf_3:paragraphs.slice(2).join('\n\n')};const result=await saveSiteSetting('sambutan_ketua_umum',payload,'Sambutan Ketua Umum PB Bilibili 162');if(result.error)throw result.error;window.dispatchEvent(new CustomEvent('sambutan_ketua_updated',{detail:payload}));await Swal.fire({icon:'success',title:'Berhasil Disimpan',text:'Isi sambutan tersimpan. Gunakan Enter 2× untuk membuat paragraf baru.',background:'#0F172A',color:'#fff',confirmButtonColor:'#2563eb'});}catch(e:any){console.error(e);Swal.fire({icon:'error',title:'Gagal Menyimpan',text:e?.message||'Data sambutan tidak tersimpan.',background:'#0F172A',color:'#fff'});}finally{setSaving(false)}};
 if(loading)return <div className="min-h-[500px] flex items-center justify-center bg-[#070d1a]"><Loader2 className="animate-spin text-blue-500" size={40}/></div>;
 return <div className="min-h-screen bg-[#070d1a] text-white p-4 sm:p-6 md:p-8 pb-24"><div className="max-w-6xl mx-auto"><div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-8"><div><p className="text-blue-400 text-[10px] font-black uppercase tracking-[0.3em] mb-2">Pengaturan Website</p><h1 className="text-2xl sm:text-4xl font-black italic uppercase tracking-tight">Edit <span className="text-blue-500">Sambutan Ketua Umum</span></h1><p className="text-slate-400 text-xs sm:text-sm mt-2">Kelola foto, nama, jabatan, dan seluruh isi sambutan dalam satu kolom.</p></div><button type="submit" form="sambutan-ketua-form" disabled={saving||uploading} className="inline-flex items-center justify-center gap-2 px-5 py-3 rounded-xl bg-blue-600 hover:bg-blue-500 disabled:opacity-50 text-white text-xs font-black uppercase tracking-wider">{saving?<Loader2 size={16} className="animate-spin"/>:<Save size={16}/>} {saving?'Menyimpan...':'Simpan Perubahan'}</button></div><form id="sambutan-ketua-form" onSubmit={handleSave} className="grid grid-cols-1 lg:grid-cols-[340px_1fr] gap-6"><section className="bg-[#0F172A] border border-white/10 rounded-2xl p-5 shadow-xl h-fit"><h2 className="text-sm font-black uppercase tracking-wider mb-4 flex items-center gap-2"><ImageIcon size={16} className="text-blue-400"/> Foto Ketua Umum</h2><div className="aspect-[4/5] rounded-2xl overflow-hidden bg-slate-900 border border-white/10 mb-4 flex items-center justify-center">{form.foto_url?<img src={form.foto_url} alt={form.nama} className="w-full h-full object-cover"/>:<ImageIcon size={42} className="text-slate-600"/>}</div><label className="w-full cursor-pointer inline-flex items-center justify-center gap-2 px-4 py-3 rounded-xl bg-slate-800 hover:bg-slate-700 border border-white/10 text-xs font-black uppercase tracking-wider">{uploading?<Loader2 size={15} className="animate-spin"/>:<Upload size={15}/>} {uploading?'Mengunggah...':'Ganti Foto Ketua'}<input type="file" accept="image/*" className="hidden" disabled={uploading} onChange={e=>{const f=e.target.files?.[0];if(f)uploadPhoto(f);e.currentTarget.value='';}}/></label></section><section className="bg-[#0F172A] border border-white/10 rounded-2xl p-5 sm:p-6 shadow-xl space-y-5"><div className="grid grid-cols-1 md:grid-cols-2 gap-4"><label className="block"><span className="text-[10px] font-black uppercase tracking-wider text-slate-400">Nama Ketua Umum</span><input value={form.nama} onChange={e=>update('nama',e.target.value)} className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 px-4 py-3 text-sm text-white outline-none focus:border-blue-500"/></label><label className="block"><span className="text-[10px] font-black uppercase tracking-wider text-slate-400">Jabatan</span><input value={form.jabatan} onChange={e=>update('jabatan',e.target.value)} className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 px-4 py-3 text-sm text-white outline-none focus:border-blue-500"/></label></div><label className="block"><span className="text-[10px] font-black uppercase tracking-wider text-slate-400">Isi Sambutan</span><textarea value={form.isi_sambutan||''} onChange={e=>update('isi_sambutan',e.target.value)} rows={16} placeholder="Tulis seluruh sambutan di sini. Tekan Enter 2× untuk membuat paragraf baru." className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 px-4 py-4 text-sm leading-relaxed text-white outline-none focus:border-blue-500 resize-y"/><p className="mt-2 text-[10px] text-slate-500">Tips: tulis semua isi dalam satu kolom. Pisahkan paragraf dengan <b className="text-slate-300">Enter 2×</b>. Landing Page akan otomatis menampilkan setiap paragraf secara terpisah.</p></label><div className="rounded-xl border border-blue-500/20 bg-blue-500/5 p-4 text-[11px] text-slate-400 leading-relaxed"><b className="text-blue-300">Sinkronisasi:</b> data disimpan di <code className="text-blue-300">site_settings → sambutan_ketua_umum</code> dan dibaca Landing Page secara realtime.</div></section></form></div></div>;
}
