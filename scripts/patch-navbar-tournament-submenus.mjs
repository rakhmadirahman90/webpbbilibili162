import fs from 'node:fs';

const path = 'src/components/Navbar.tsx';
let src = fs.readFileSync(path, 'utf8');

// Never hide the seeded participant item. It is a real public submenu.
src = src.replace(/\n\s*const visibleList = list\.filter\(i => \{[\s\S]*?\n\s*\}\);\n\s*if \(!visibleList\.length && \(parent\?\.path === 'atlet' \|\| parent\?\.label\?\.toLowerCase\(\) === 'atlet'\)\) return ATLET_DEFAULT_SUBMENUS;\n\s*return visibleList;/, `\n    // Keep the tournament submenu complete even when an older cached navbar\n    // payload is missing one of the managed entries.\n    const isTournament = parent?.path === 'pendaftaran-turnamen' || String(parent?.label || '').toLowerCase() === 'pendaftaran peserta';\n    if (isTournament) {\n      const defaults = [\n        { id: 'tournament-form-default', label: 'Formulir Pendaftaran Turnamen', path: 'pendaftaran-turnamen', type: 'link', parent_id: parent?.id, order_index: 1 },\n        { id: 'tournament-seeded-default', label: 'Daftar Seeded Peserta', path: 'pendaftaran/seeded-peserta', type: 'link', parent_id: parent?.id, order_index: 2 },\n        { id: 'tournament-accepted-default', label: 'Daftar Peserta Diterima', path: 'pendaftaran/peserta-diterima', type: 'link', parent_id: parent?.id, order_index: 3 },\n        { id: 'tournament-sponsor-default', label: 'Daftar Sponsorship', path: 'sponsorship', type: 'link', parent_id: parent?.id, order_index: 4 },\n      ];\n      const keys = (item: any) => {\n        const p = normalizeNavigationPath(item?.path || '');\n        const l = String(item?.label || '').toLowerCase().trim();\n        return p || l;\n      };\n      const merged = [...list];\n      for (const fallback of defaults) {\n        const exists = merged.some((item: any) => keys(item) === keys(fallback) || (fallback.path === 'sponsorship' && (keys(item).includes('sponsor') || keys(item).includes('sponsorship'))) || (fallback.path === 'pendaftaran/seeded-peserta' && (keys(item).includes('seeded-peserta') || keys(item).includes('daftar-seeded'))));\n        if (!exists) merged.push(fallback);\n      }\n      return merged.sort((a: any, b: any) => (a.order_index || 0) - (b.order_index || 0));\n    }\n    if (!list.length && (parent?.path === 'atlet' || parent?.label?.toLowerCase() === 'atlet')) return ATLET_DEFAULT_SUBMENUS;\n    return list;`);

// Ensure route preloading understands every tournament submenu.
src = src.replace("                                  : effective === 'pendaftaran-turnamen'\n                                    ? '/pendaftaran-turnamen'\n                                    : null;", "                                  : (effective === 'pendaftaran-turnamen' || effective === 'pendaftaran/seeded-peserta' || effective === 'pendaftaran/peserta-diterima' || effective === 'sponsorship')\n                                    ? (effective === 'sponsorship' ? '/sponsorship' : effective === 'pendaftaran/seeded-peserta' ? '/pendaftaran/seeded-peserta' : effective === 'pendaftaran/peserta-diterima' ? '/pendaftaran/peserta-diterima' : '/pendaftaran-turnamen')\n                                    : null;");

fs.writeFileSync(path, src, 'utf8');
console.log('[patch-navbar-tournament-submenus] tournament submenu forced complete for desktop/mobile');
