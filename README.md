pbus162bilibili

Popup production sync: Supabase konfigurasi_popup is authoritative; popup reads and realtime updates are synchronized with the database.

Build verification: authoritative popup patch is build-safe and does not depend on AdminPopup persistPopups.
